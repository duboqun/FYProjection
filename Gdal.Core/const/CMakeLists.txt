cmake_minimum_required(VERSION 3.13)
project(gdalconst_warp)
add_library(gdalconst_warp SHARED gdalconst_wrap.c)
find_package(GDAL)
include_directories(${GDAL_INCLUDE_DIR})
target_link_libraries(gdalconst_warp ${GDAL_LIBRARY})