cmake_minimum_required(VERSION 3.13)
project(gdal_warp)
add_library(gdal_warp SHARED gdal_wrap.cpp)
find_package(GDAL)
include_directories(${GDAL_INCLUDE_DIR})
target_link_libraries(gdal_warp ${GDAL_LIBRARY})